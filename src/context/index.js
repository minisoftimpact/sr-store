import { createContext } from "react";

const CartContext = createContext(null);

const ModalContext = createContext(null);

const NavigationContext = createContext(null);

export { CartContext, ModalContext, NavigationContext };
